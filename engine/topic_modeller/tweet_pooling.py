'''

    Extract top 100 entities.
    Find all tweets containing the entity for each entity.
    Dump each set into a separate collection.

    Make sure the collection names can be easily accessed by lda.py and tweet_segregation.py
    Also the names should distinguish between hashtag and mentions.

'''